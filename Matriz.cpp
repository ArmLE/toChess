#include "Matriz.h"
#include <iostream>
/*
template <typename DataType>
Matriz<DataType>::Matriz():
    data{MatrizType(0, RowType(0,{}))}
//data{MatrizType(Rows, RowType(Columns,{}))}
{

}

template <typename DataType>
Matriz<DataType>::Matriz(const std::size_t& rows,const std::size_t& cols):
    data{MatrizType(rows, RowType(cols,{}))}
{

}


template <typename DataType>
const typename Matriz<DataType>::RowType& Matriz<DataType>::operator[](unsigned rowIndex) const{
    return data[rowIndex];
}
template <typename DataType>
typename Matriz<DataType>::RowType& Matriz<DataType>::operator[](unsigned rowIndex) {
    return data[rowIndex];
}
*/


